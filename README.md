# product

複数プロジェクトを管理するモノレポ。

## ディレクトリ構造

```
projects/
└── delne/         認知症者向けAI電話サービス「DELNE」
```

各プロジェクトの詳細は `projects/<name>/README.md` を参照。

## 運用ルール

- 新規プロジェクトは `projects/<project-name>/` を作成して追加する
- 横断的な設定 (`.gitignore` 等) のみリポジトリルートに置く
- プロジェクト固有のドキュメント・コード・成果物は各プロジェクトディレクトリ内で完結させる
